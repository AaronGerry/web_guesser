<!DOCTYPE html>
<html>
  <head>
    <title>Number Guesser</title>
    <link rel="stylesheet" href="css/foundation.css">
    <link rel="stylesheet" href="stylesheets/style.css">
  </head>

  <body>
    <div class="top-bar">
      <h1>Let's play a game...</h1>
    </div>

    <br>

    <div class="small-12 medium-4 columns small-centered large-centered text-center">
      <div class="callout primary">
        <div class="float-center">
          <h3>Time for you to guess!...</h3>
        </div>
        <form>
          <label>a number between 0-100:
            <input type="text" name="guess" />
            <input type="submit" value="submit my guess!" />
          </label>
        </form>
      </div>
    </div>

    <div class="small-12 medium-4 columns small-centered large-centered text-center">
      <div class="callout">
        <p>
          <% if message == "Let's begin!" %>
            <%= message %>
          <% elsif message == "You got it right! Go you!" %>
            <div class="callout results win">
              <%= message %>
            </div>
          <% elsif message == "Way too low!" || message == "Way too high!" %>
            <div class="callout results way">
              <%= message %>
            </div>
          <% elsif message = "Too low!" || message == "Too high!" %>
            <div class="callout results close">
              <%= message %>
            </div>
          <% end %>
        </p>
        <p>
          <% if message == "You got it right! Go you!" %>
            (Your SECRET number is: <%= number %>)

            <br>

            <a href="/replay">Want to play again?</a>
          <% end %>
        </p>
      </div>
    </div>

  </body>
</html>
